<%= simple_form_for @restaurant do |r| %>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :phone_number %>
  <%= r.input :category, collection: Restaurant::TYPE %>
  <%= r.button :submit, "Create Restaurant", class: "btn btn-primary" %>
<% end %>
